name: CI

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  lit:
    name: Lit tests (LLVM ${{ matrix.llvm-version }})
    runs-on: ubuntu-24.04

    strategy:
      fail-fast: false
      matrix:
        llvm-version: ["20", "21", "22"]

    env:
      LLVM_VERSION: ${{ matrix.llvm-version }}
      LLVM_ROOT: /usr/lib/llvm-${{ matrix.llvm-version }}
      RUSTUP_TOOLCHAIN: "1.96.0"
      CARGO_TERM_COLOR: always

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Install LLVM/MLIR dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y ca-certificates gpg wget
          sudo install -d -m 0755 /etc/apt/keyrings
          wget -qO- https://apt.llvm.org/llvm-snapshot.gpg.key \
            | sudo gpg --dearmor -o /etc/apt/keyrings/apt.llvm.org.gpg
          echo "deb [signed-by=/etc/apt/keyrings/apt.llvm.org.gpg] http://apt.llvm.org/noble/ llvm-toolchain-noble-${LLVM_VERSION} main" \
            | sudo tee /etc/apt/sources.list.d/llvm${LLVM_VERSION}.list
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            clang-${LLVM_VERSION} \
            cmake \
            lld-${LLVM_VERSION} \
            libmlir-${LLVM_VERSION}-dev \
            llvm-${LLVM_VERSION}-dev \
            llvm-${LLVM_VERSION}-tools \
            mlir-${LLVM_VERSION}-tools \
            ninja-build \
            python3 \
            python3-pip \
            python3-venv

      - name: Install Python tools
        run: |
          sudo python3 -m pip install --break-system-packages lit
          python3 -m venv .venv
          .venv/bin/python -m pip install --upgrade pip
          .venv/bin/python -m pip install auditwheel hatchling patchelf
          lit --version
          echo "LLVM_EXTERNAL_LIT=$(command -v lit)" >> "${GITHUB_ENV}"

      - name: Install Rust toolchain
        run: |
          rustup toolchain install "${RUSTUP_TOOLCHAIN}" \
            --profile minimal \
            --component cargo \
            --component llvm-tools-preview \
            --component rust-src \
            --component rustc-dev
          rustup show active-toolchain
          cargo --version
          rustc --version
          echo "RUST_TO_MLIR_CARGO_EXECUTABLE=$(rustup which cargo)" >> "${GITHUB_ENV}"
          echo "RUST_TO_MLIR_RUSTC_EXECUTABLE=$(rustup which rustc)" >> "${GITHUB_ENV}"

      - name: Configure
        run: |
          cmake -S . -B build -G Ninja \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_C_COMPILER=clang-${LLVM_VERSION} \
            -DCMAKE_CXX_COMPILER=clang++-${LLVM_VERSION} \
            -DMLIR_DIR=${LLVM_ROOT}/lib/cmake/mlir \
            -DLLVM_DIR=${LLVM_ROOT}/lib/cmake/llvm \
            -DLLVM_EXTERNAL_LIT=${LLVM_EXTERNAL_LIT} \
            -DRUST_TO_MLIR_CARGO_EXECUTABLE=${RUST_TO_MLIR_CARGO_EXECUTABLE} \
            -DRUST_TO_MLIR_RUSTC_EXECUTABLE=${RUST_TO_MLIR_RUSTC_EXECUTABLE} \
            -DRUST_TO_MLIR_PYTHON_EXECUTABLE=${GITHUB_WORKSPACE}/.venv/bin/python

      - name: Build
        run: ninja -C build

      - name: Run lit tests
        run: ninja -C build check-rust-to-mlir

      - name: Build wheel
        run: ninja -C build wheel

      - name: Upload wheel
        uses: actions/upload-artifact@v4
        with:
          name: rust-to-mlir-wheel-llvm${{ matrix.llvm-version }}
          path: dist/*.whl
          if-no-files-found: error
